name: nextjs
type: "nodejs:20"
hooks:
  build: |
    npm install
    npm run build
web:
  commands:
    start: "npm run start"
  locations:
    "/":
      passthru: true
relationships:
  database: "db:mariadb" 
workers:
  background:
    commands:
      start: "node app/worker.js"
